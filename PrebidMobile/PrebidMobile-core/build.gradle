apply from: '../android.gradle'
apply from: '../publisher.gradle'
apply from: '../tests.gradle'
apply plugin: 'org.jetbrains.kotlin.android'

android {
    namespace "org.prebid.mobile.core"

    buildFeatures {
        buildConfig true
    }

    def getGitHash = { ->
        def stdout = new ByteArrayOutputStream()
        exec {
            commandLine 'git', 'rev-parse', '--short', 'HEAD'
            standardOutput = stdout
        }
        return stdout.toString().trim()
    }
    kotlinOptions {
        jvmTarget = '17'
    }

    defaultConfig {
        buildConfigField "String", "GitHash", "\"${getGitHash()}\""
        buildConfigField "String", "VERSION", "\"${versionName}\""
        buildConfigField "String", "OMSDK_VERSION", "\"${omSdkVersion}\""
        multiDexEnabled true
    }
    testOptions {
        unitTests.all {
            jvmArgs '--add-opens=java.base/java.lang=ALL-UNNAMED'
            systemProperty 'robolectric.invokedynamic.enable', 'false'
        }
    }
}


dependencies {
    implementation project(":omsdk-android")

    implementation libs.androidx.annotation

    implementation libs.guava
    implementation libs.concurrent.futures

    implementation libs.google.play.services.base
    implementation libs.google.play.services.ads
    implementation libs.google.play.services.ads.identifier

    // Next-Gen GMA SDK (used by api/multiloadernextgen GAM loaders). compileOnly so it does NOT
    // land on core's unit-test classpath: it bundles a final com.google.android.gms.ads.AdError
    // that conflicts with legacy play-services-ads' AdError/LoadAdError used by the multiadloader
    // tests. Consumers of multiloadernextgen add the Next-Gen SDK themselves (see PrebidVeonNextGenDemo);
    // its unit tests live in :PrebidMobile-nextGenEventHandlers where the SDK is on the classpath.
    compileOnly libs.google.next.gen.sdk

    implementation libs.exoplayer
    implementation libs.exoplayer.ui

    implementation libs.androidx.localbroadcastmanager

}

configurations.all {
    resolutionStrategy.eachDependency { details ->
        if (details.requested.group == 'com.google.guava' && details.requested.name == 'listenablefuture') {
            details.useTarget('androidx.concurrent:concurrent-futures:1.1.0')
            details.because('Use androidx.concurrent.futures instead of guava listenablefuture to avoid conflicts')
        }
    }
}

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release
                groupId = "org.prebid.mobile"
                artifactId = "core"
                version = '1.0.0'
            }
        }
    }
}